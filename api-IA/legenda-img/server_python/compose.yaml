services:
  server-python:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: server-python

    ports:
      - "5000:5000"

    volumes:
      - .:/app

    environment:
      - PYTHONUNBUFFERED=1

    command: uv run flask --app main.py run --host=0.0.0.0
